# OpenShift Message Of The Day

- This message appears when using "oc login" from the CLI. It does not configure the web console splash screen which is done with another policy

```console
oc create -f motd.yaml
```
