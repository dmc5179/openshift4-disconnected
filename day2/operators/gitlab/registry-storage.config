s3:
  region: 
  bucket: prefix-gitlab-registry-storage
  accesskey: 
  secretkey: 
  encrypt: true
  #kmskeyid:   # KMS ARN if used
  #sse_kms_key_id: # KMS ARN if used
  v4auth: true
cache:
  blobdescriptor: inmemory
