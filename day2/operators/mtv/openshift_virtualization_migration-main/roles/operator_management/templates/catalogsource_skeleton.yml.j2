apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: {{ _catalogsource_item.value.metadata.name | infra.openshift_virtualization_migration.rfc1123 }}
  namespace: {{ _catalogsource_item.value.metadata.namespace | d('openshift-marketplace', true) }}
spec:
  sourceType: grpc
