[all:vars]
ansible_user=root 
ansible_ssh_private_key_file="{{ ansible_env.HOME }}/.ssh/id_rsa"
#ansible_become=True

openshift_kubeconfig_path="{{ ansible_env.HOME }}/.kube/config"

[new_workers] 
# Add new worker node's hostname as it will appear in the cluster
