---
# defaults file for fetch_roles

staging_dir: '/tmp/ansible_roles'

ansible_roles:
  - bertvv.bind
  - bertvv.dhcp
  - bertvv.tftp
  - mariuszczyz.centos_pxe

air_gapped: true

mirror_registry: 'localhost:5000'
mirror_transport: 'https'
