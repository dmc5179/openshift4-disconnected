---

# These are located under the files directory in this role
root_ca: 'root_ca.pem'
ingress_cert: 'ingress_cert.pem'
full_chain: 'full_chain.pem'
ingress_key: 'ingress_cert_key.pem'

kubeconfig: "{{ ansible_env.HOME }}/.kube/config"
kube_ssl_enabled: false
